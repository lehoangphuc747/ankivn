---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const guides = await getCollection('guides');

// Group guides by type - Only Cơ bản and Nâng cao
const guidesByType = {
  'Cơ bản': guides.filter(g => g.data.guideType === 'Cơ bản'),
  'Nâng cao': guides.filter(g => g.data.guideType === 'Nâng cao')
};

// Calculate statistics for only these two types
const allValidGuides = [...guidesByType['Cơ bản'], ...guidesByType['Nâng cao']];
const totalCourses = allValidGuides.length;
const totalLessons = allValidGuides.reduce((acc, guide) => {
  return acc + (guide.data.modules?.reduce((lessonAcc, module) => lessonAcc + module.lessons.length, 0) || 1);
}, 0);

// Improved duration calculation to handle 'giờ' and 'phút'
const totalDuration = allValidGuides.reduce((acc, guide) => {
  const duration = (guide.data as any).duration || "30 phút";
  const value = parseFloat(duration.match(/(\d+\.?\d*)/)?.[1] || "0");
  if (duration.includes('giờ')) {
    return acc + value;
  } else if (duration.includes('phút')) {
    return acc + value / 60;
  }
  return acc + value; // Assume hours if no unit
}, 0).toFixed(0);
---

<BaseLayout title="Anki Academy - Học viện Anki trực tuyến">
  <div class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50/30 to-green-50/20">
    
    <!-- Hero Section with Enhanced Design -->
    <section class="relative overflow-hidden">
      <!-- Background decoration -->
      <div class="absolute inset-0 bg-gradient-to-r from-blue-600/5 to-green-600/5"></div>
      
      <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="text-center">
          <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-500 to-green-500 rounded-2xl mb-6 shadow-lg">
            <span class="text-3xl">📚</span>
          </div>
          <h1 class="text-5xl md:text-6xl font-black text-gray-900 mb-6 leading-tight">
            Anki Academy
          </h1>
          <p class="text-xl md:text-2xl text-gray-600 mb-8 max-w-3xl mx-auto leading-relaxed font-medium">
            Học viện trực tuyến về Anki - Từ cơ bản đến chuyên gia
          </p>
        </div>
      </div>
    </section>

    <!-- Stats Section -->
    <section class="relative -mt-16 z-10">
      <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 bg-white/80 backdrop-blur-sm rounded-3xl p-8 border border-white/40 shadow-xl">
          <div class="text-center">
            <div class="text-4xl font-black bg-gradient-to-r from-blue-600 to-green-600 bg-clip-text text-transparent">{totalCourses}</div>
            <div class="text-lg text-gray-600 font-medium">Khóa học</div>
          </div>
          <div class="text-center">
            <div class="text-4xl font-black bg-gradient-to-r from-blue-600 to-green-600 bg-clip-text text-transparent">{totalLessons}</div>
            <div class="text-lg text-gray-600 font-medium">Bài học</div>
          </div>
          <div class="text-center">
            <div class="text-4xl font-black bg-gradient-to-r from-blue-600 to-green-600 bg-clip-text text-transparent">
              {totalDuration}
              <span class="text-2xl text-gray-500">+</span>
            </div>
            <div class="text-lg text-gray-600 font-medium">Giờ học</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Learning Paths with Enhanced Cards -->
    <section class="relative">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="text-center mb-16">
          <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">Chọn lộ trình học tập</h2>
          <div class="w-24 h-1 bg-gradient-to-r from-blue-500 to-green-500 mx-auto rounded-full"></div>
        </div>

        <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
          <!-- Beginner Path with Enhanced Design -->
          <a href="/guides/co-ban" class="group relative bg-white/80 backdrop-blur-sm border border-green-100 rounded-3xl p-10 hover:bg-green-50 hover:border-green-200 hover:shadow-2xl hover:scale-[1.02] transition-all duration-500 cursor-pointer overflow-hidden">
            <!-- Background pattern -->
            <div class="absolute top-0 right-0 w-32 h-32 bg-gradient-to-br from-green-100/50 to-transparent rounded-full -translate-y-16 translate-x-16 group-hover:scale-150 transition-transform duration-700"></div>
            
            <div class="relative text-center">
              <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-green-400 to-green-600 rounded-2xl mb-6 group-hover:scale-110 group-hover:rotate-3 transition-all duration-300 shadow-lg">
                <span class="text-3xl">🌱</span>
              </div>
              <h3 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4 group-hover:text-green-600 transition-colors duration-300">Cơ bản</h3>
              <p class="text-lg text-gray-600 leading-relaxed font-medium mb-4">Bắt đầu từ con số 0, học các khái niệm cơ bản.</p>
              <div class="inline-flex items-center px-3 py-1 bg-green-100 text-green-700 text-sm font-semibold rounded-full">
                {guidesByType['Cơ bản'].length} khóa học
              </div>
            </div>
          </a>

          <!-- Advanced Path with Enhanced Design -->
          <a href="/guides/nang-cao" class="group relative bg-white/80 backdrop-blur-sm border border-blue-100 rounded-3xl p-10 hover:bg-blue-50 hover:border-blue-200 hover:shadow-2xl hover:scale-[1.02] transition-all duration-500 cursor-pointer overflow-hidden">
            <!-- Background pattern -->
            <div class="absolute top-0 right-0 w-32 h-32 bg-gradient-to-br from-blue-100/50 to-transparent rounded-full -translate-y-16 translate-x-16 group-hover:scale-150 transition-transform duration-700"></div>
            
            <div class="relative text-center">
              <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-400 to-blue-600 rounded-2xl mb-6 group-hover:scale-110 group-hover:rotate-3 transition-all duration-300 shadow-lg">
                <span class="text-3xl">🚀</span>
              </div>
              <h3 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4 group-hover:text-blue-600 transition-colors duration-300">Nâng cao</h3>
              <p class="text-lg text-gray-600 leading-relaxed font-medium mb-4">Kỹ thuật nâng cao và tối ưu hóa</p>
              <div class="inline-flex items-center px-3 py-1 bg-blue-100 text-blue-700 text-sm font-semibold rounded-full">
                {guidesByType['Nâng cao'].length} khóa học
              </div>
            </div>
          </a>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>
