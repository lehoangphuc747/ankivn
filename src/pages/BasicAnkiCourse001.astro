---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Đăng Ký Khóa Học Anki Cơ Bản" description="Đăng ký tham gia khóa học Anki cơ bản để học cách ghi nhớ hiệu quả.">
  <style>
    .container {
      max-width: 900px;
      margin: 2rem auto;
      padding: 2.5rem;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      border: 1px solid #e5e7eb;
    }

    h1 {
      text-align: center;
      color: #1e293b;
      margin-bottom: 2.5rem;
      font-size: 2.2rem;
      font-weight: 700;
      letter-spacing: -0.025em;
    }

    h1 small {
      font-size: 1rem;
      font-weight: 400;
      color: #64748b;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }

    .form-group {
      margin-bottom: 1.75rem;
    }

    label {
      display: block;
      margin-bottom: 0.75rem;
      font-weight: 600;
      color: #1f2937;
      font-size: 1rem;
    }

    input {
      width: 100%;
      padding: 14px 16px;
      border: 2px solid #d1d5db;
      border-radius: 8px;
      font-size: 16px;
      box-sizing: border-box;
      background-color: #ffffff;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    .form-group.error input {
      border-color: #ef4444;
      box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.1);
    }

    .error-message {
      color: #ef4444;
      font-size: 0.875rem;
      margin-top: 0.25rem;
      display: none;
    }

    .form-group.error .error-message {
      display: block;
    }

    .submit-btn {
      width: 100%;
      background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
      color: white;
      border: none;
      padding: 16px 20px;
      border-radius: 8px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      margin-top: 1.5rem;
      transition: all 0.2s ease;
      box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
    }

    .submit-btn:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }

    .submit-btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
      transform: none;
      box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
    }

    .loading {
      display: none;
      text-align: center;
      margin-top: 1.5rem;
      padding: 1.5rem;
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border-radius: 8px;
      border: 2px solid #f59e0b;
      color: #92400e;
      font-weight: 600;
    }

    .about {
      margin: 2rem 0;
      padding: 0;
      color: #1e293b;
      line-height: 1.7;
    }

    .session-info {
      margin: 2rem 0;
      padding: 2rem;
      background: linear-gradient(135deg, #fef7ff 0%, #fdf4ff 100%);
      border-radius: 12px;
      border: 2px solid #c084fc;
      color: #1e293b;
    }

    .session-info h2 {
      margin: 0 0 1.5rem 0;
      color: #7c3aed;
      font-size: 1.25rem;
      font-weight: 700;
      text-align: center;
    }

    .info-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .info-item {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .info-icon {
      font-size: 1.5rem;
      flex-shrink: 0;
    }

    .info-content {
      line-height: 1.5;
      color: #374151;
    }

    .info-note {
      margin: 0;
      padding: 1rem;
      background: rgba(255, 255, 255, 0.7);
      border-radius: 8px;
      border-left: 4px solid #c084fc;
      font-style: italic;
      color: #4b5563;
      line-height: 1.6;
    }

    .form-section {
      margin-top: 2rem;
    }

    .about h2 {
      margin: 0 0 1rem 0;
      color: #0f172a;
      font-size: 1.25rem;
      font-weight: 700;
    }

    .about ul {
      margin: 0.75rem 0 1rem 1.5rem;
      padding: 0;
    }

    .advanced-topics {
      margin-top: 3rem;
      padding: 1.5rem;
      background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
      border-radius: 12px;
      border: 2px solid #0ea5e9;
      color: #0c4a6e;
    }

    .advanced-topics h3 {
      margin: 0 0 1rem 0;
      color: #0c4a6e;
      font-size: 1.1rem;
      font-weight: 700;
    }

    .advanced-topics ul {
      margin: 0;
      padding-left: 1.5rem;
    }

    .advanced-topics li {
      margin-bottom: 0.75rem;
      line-height: 1.5;
      color: #374151;
    }

    .advanced-topics li:last-child {
      margin-bottom: 0;
    }

    .spinner {
      border: 4px solid #fef3c7;
      border-top: 4px solid #f59e0b;
      border-radius: 50%;
      width: 28px;
      height: 28px;
      animation: spin 1s linear infinite;
      display: inline-block;
      margin-right: 12px;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    @media (max-width: 768px) {
      .container {
        margin: 1rem;
        padding: 2rem;
        max-width: none;
      }

      .about {
        margin: 1.5rem 0;
      }

      .session-info {
        margin: 1.5rem 0;
        padding: 1.5rem;
      }

      .info-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }

      .form-section {
        margin-top: 1.5rem;
      }
    }
  </style>

  <div class="container">
    <h1>BUỔI HƯỚNG DẪN ANKI CƠ BẢN<br><small>(BASIC ANKI COURSE)</small></h1>

    <!-- About Section - Xây dựng niềm tin -->
    <section class="about-section">
      <div class="about" id="about">
        <h2>✨ Về mình</h2>
        <p>Với hơn 3 năm kinh nghiệm, mình đã trực tiếp hỗ trợ hơn 100 người Việt đang:</p>
        <ul>
          <li>Sinh sống tại nhiều quốc gia: Việt Nam, Nhật Bản, Hàn Quốc, Cộng hòa Czech, Mông Cổ, Úc, Mỹ, ….</li>
          <li>Nhiều lĩnh vực và chuyên ngành: Học tiếng Anh, tiếng Trung, tiếng Hàn, tiếng Nhật (khó nhất); Cơ khí, hàng hải, kiểm toán, v.v...</li>
        </ul>
        <p>→ Dù bạn học ngoại ngữ hay chuyên ngành kỹ thuật, Anki đều có thể giúp bạn ghi nhớ hiệu quả hơn – nếu biết cách dùng đúng.</p>
      </div>
    </section>

    <!-- Session Info Section -->
    <section class="session-info">
      <h2>ℹ️ THÔNG TIN BUỔI HƯỚNG DẪN</h2>
      <div class="info-grid">
        <div class="info-item">
          <span class="info-icon">⏰</span>
          <div class="info-content">
            <strong>Thời gian:</strong> 19:30, Chủ Nhật
          </div>
        </div>
        <div class="info-item">
          <span class="info-icon">⏳</span>
          <div class="info-content">
            <strong>Thời lượng:</strong> ~90 phút
          </div>
        </div>
        <div class="info-item">
          <span class="info-icon">📍</span>
          <div class="info-content">
            <strong>Hình thức:</strong> Online (Link tham gia sẽ gửi về email đã đăng ký)
          </div>
        </div>
        <div class="info-item">
          <span class="info-icon">💰</span>
          <div class="info-content">
            <strong>Phí:</strong> tuỳ tâm, hổng gửi cũng được :)))
          </div>
        </div>
      </div>
      <p class="info-note">Video về quá trình học sẽ được chỉnh sửa và đăng lại lên trang Anki Việt Nam để kiến thức có thể được chia sẻ lại.</p>
    </section>

    <!-- Registration Form - Convert action -->
    <div class="form-section">
      <form id="registrationForm" method="POST" action="https://docs.google.com/forms/d/e/1FAIpQLSd2jGXkLCS7tnQnSF7BkUogZepUQqd0_E8jELZnqflcQB-sqw/formResponse">
        <div class="form-group">
          <label for="name">Họ và tên *</label>
          <input type="text" id="name" name="entry.1884849361" placeholder="Ví dụ: Lê Hoàng Phúc" required>
          <div class="error-message">Vui lòng nhập họ và tên</div>
        </div>

        <div class="form-group">
          <label for="facebook">Facebook *</label>
          <input type="text" id="facebook" name="entry.491379967" placeholder="Ví dụ: fb.com/tuilaphuc747" required>
          <div class="error-message">Vui lòng nhập Facebook</div>
        </div>

        <div class="form-group">
          <label for="email">Gmail *</label>
          <input type="email" id="email" name="entry.1002519589" placeholder="Ví dụ: ankivn.official@gmail.com" required>
          <small style="color: #6b7280; font-size: 0.875rem; margin-top: 0.25rem; display: block;">Nhập gmail để được cấp quyền truy cập vào buổi hướng dẫn</small>
          <div class="error-message">Vui lòng nhập Gmail hợp lệ</div>
        </div>

        <button type="submit" class="submit-btn" id="submitBtn">
          🚀 Đăng Ký Ngay
        </button>
      </form>

      <div class="loading" id="loading">
        <div class="spinner"></div>
        <strong>Đang gửi đăng ký...</strong>
      </div>
    </div>

    <!-- Advanced Topics Teaser - Tạo sự tò mò về tương lai -->
    <div class="advanced-topics">
      <h3>🚀 SẮP TỚI - CÁC KHÓA HỌC NÂNG CAO</h3>
      <p style="margin-bottom: 1rem; color: #374151; font-style: italic;">Đăng ký buổi hướng dẫn cơ bản ngay để không bỏ lỡ các khóa học nâng cao sắp tới!</p>
      <ul>
        <li>🤖 Tạo thẻ hàng loạt với AI & Google Sheets</li>
        <li>🎨 Tạo mẫu thẻ bằng AI (cho người không biết code)</li>
        <li>💻 Anki Portable – học mọi lúc, mọi nơi, không cần cài lại</li>
        <li>🧠 Hệ thống hóa kiến thức với Obsidian + Anki (phù hợp với người muốn xây dựng hệ thống học sâu và cá nhân hóa)</li>
      </ul>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.getElementById('registrationForm') as HTMLFormElement;
      const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement;
      const loading = document.getElementById('loading') as HTMLElement;
      const nameInput = document.getElementById('name') as HTMLInputElement;
      const facebookInput = document.getElementById('facebook') as HTMLInputElement;
      const emailInput = document.getElementById('email') as HTMLInputElement;

      function validateField(input: HTMLInputElement) {
        const formGroup = input.closest('.form-group') as HTMLElement;
        const value = input.value.trim();

        if (input === emailInput) {
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if (!value || !emailRegex.test(value)) {
            formGroup.classList.add('error');
            return false;
          } else {
            formGroup.classList.remove('error');
            return true;
          }
        } else {
          if (!value) {
            formGroup.classList.add('error');
            return false;
          } else {
            formGroup.classList.remove('error');
            return true;
          }
        }
      }

      function validateForm() {
        const nameValid = validateField(nameInput);
        const facebookValid = validateField(facebookInput);
        const emailValid = validateField(emailInput);

        return nameValid && facebookValid && emailValid;
      }

      // Add blur event listeners to validate on field leave
      nameInput.addEventListener('blur', () => validateField(nameInput));
      facebookInput.addEventListener('blur', () => validateField(facebookInput));
      emailInput.addEventListener('blur', () => validateField(emailInput));

      // Clear errors when user starts typing
      nameInput.addEventListener('input', () => {
        if (nameInput.value.trim()) {
          nameInput.closest('.form-group')?.classList.remove('error');
        }
      });

      facebookInput.addEventListener('input', () => {
        if (facebookInput.value.trim()) {
          facebookInput.closest('.form-group')?.classList.remove('error');
        }
      });

      emailInput.addEventListener('input', () => {
        if (emailInput.value.trim()) {
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if (emailRegex.test(emailInput.value.trim())) {
            emailInput.closest('.form-group')?.classList.remove('error');
          }
        }
      });

      form.addEventListener('submit', function(e) {
        if (!validateForm()) {
          e.preventDefault();
          return false;
        }

        submitBtn.disabled = true;
        submitBtn.innerHTML = '<span class="spinner"></span> Đang gửi...';
        loading.style.display = 'block';
        // Form will submit normally to Google Forms
      });
    });
  </script>
</BaseLayout>