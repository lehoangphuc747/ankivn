---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SharingButtons from '@/components/common/SharingButtons.astro';

const { 
  title, 
  description,
  publishDate, 
  author, 
  authorBio,
  tags, 
  source,
  readingTime, 
  cover,
  category
} = Astro.props as { 
  title: string; 
  description?: string;
  publishDate?: Date | string; 
  author?: string;
  authorBio?: string;
  source?: string;
  tags?: string[];
  readingTime?: string;
  cover?: string;
  category?: string;
};

// Safe defaults
const safeTags = tags || [];
const safeSource = source || null;
const formattedDate = publishDate ? new Date(publishDate).toLocaleDateString('vi-VN', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
}) : '';

const getCategoryColor = (cat?: string) => {
  switch (cat?.toLowerCase()) {
    case 'tutorial': return 'bg-blue-100 text-blue-800';
    case 'tips': return 'bg-green-100 text-green-800';
    case 'news': return 'bg-purple-100 text-purple-800';
    case 'review': return 'bg-orange-100 text-orange-800';
    default: return 'bg-gray-100 text-gray-800';
  }
};
---

<BaseLayout title={title} description={description}>
  <main class="min-h-screen">
    <!-- Content -->
    <section class="py-16">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-white rounded-xl shadow-lg">
          <!-- Article Header -->
          <div class="border-b border-gray-200 p-8 md:p-12">
            <!-- Title - Có thể điều chỉnh font size theo ý muốn: text-xl, text-2xl, text-3xl, text-4xl, etc. -->
            <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4">
              {title}
            </h1>
            
            <!-- Meta Info -->
            <div class="flex flex-wrap items-center gap-4 text-sm text-gray-600 mb-4">
              
              {formattedDate && (
                <div class="flex items-center gap-1">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                  </svg>
                  <span>{formattedDate}</span>
                </div>
              )}

            </div>
            
            <!-- Tags -->
            {safeTags.length > 0 && (
              <div class="flex flex-wrap gap-2">
                {safeTags.map((tag) => (
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-gray-100 text-gray-800">
                    #{tag}
                  </span>
                ))}
              </div>
            )}
          </div>
          
          <!-- Article Content -->
          <article class="content-article p-8 md:p-12">
            <div class="markdown-content">
              <slot />
            </div>
          </article>
          
          <!-- Author Bio Section -->
          {author && (
            <div class="border-t border-gray-200 p-8 md:p-12">
              <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6 bg-gradient-to-r from-gray-50 to-slate-50 rounded-xl p-6 transition-all duration-300 hover:shadow-lg hover:border-gray-200 border border-transparent">
                <div class="flex items-center gap-4">
                  <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-slate-600 to-slate-800 rounded-full flex items-center justify-center text-white font-bold text-xl shadow-md">
                    {author.charAt(0).toUpperCase()}
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Tác giả</p>
                    <p class="text-lg font-semibold text-gray-800">{author}</p>
                    {authorBio && (
                      <p class="text-sm text-gray-600 mt-1">{authorBio}</p>
                    )}
                  </div>
                </div>

                <div class="flex items-center gap-3">
                  {safeSource ? (
                    <a href={safeSource} target="_blank" rel="noopener noreferrer" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors text-sm font-medium flex-shrink-0 shadow-md hover:shadow-lg transform hover:-translate-y-0.5" title="Mở nguồn bài viết trong tab mới">
                      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
                      Mở nguồn bài viết
                    </a>
                  ) : (
                    <button class="inline-flex items-center px-4 py-2 bg-gray-200 text-gray-500 rounded-lg text-sm font-medium flex-shrink-0 cursor-not-allowed" aria-disabled="true" title="Bài viết không có nguồn tham khảo">
                      <svg class="w-4 h-4 mr-2 opacity-60" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
                      Không có nguồn
                    </button>
                  )}
                </div>
              </div>
            </div>
          )}
        </div>
        
        <!-- Sharing Section -->
        <div class="mt-8">
          <SharingButtons 
            url={Astro.url.toString()} 
            title={title}
            description={`Đọc bài viết: ${title}. Tìm hiểu thêm về Anki và phương pháp học tập hiệu quả tại AnkiVN!`}
            shareTitle="Chia sẻ bài viết"
          />
        </div>
      </div>
    </section>

    <!-- Navigation -->
    <section class="py-12 bg-white border-t border-gray-200">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
          <a href="/blog" class="flex items-center px-6 py-3 bg-white text-gray-700 rounded-lg hover:bg-gray-50 transition-colors border border-gray-200">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            Quay lại blog
          </a>
          
          <div class="flex gap-4">
            <!-- Social Share Buttons -->
            <button class="flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors text-sm">
              <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd"></path>
              </svg>
              Chia sẻ
            </button>
          </div>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  /* Custom blog-specific styles */
  .prose {
    /* Global prose styling sẽ được áp dụng từ global.css */
    max-width: none;
  }
  
  /* Enhanced readability for blog posts */
  .prose h1, .prose h2, .prose h3, .prose h4 {
    color: #1f2937;
    scroll-margin-top: 2rem;
  }
  
  .prose p {
    margin-bottom: 1.5rem;
    line-height: 1.75;
  }
  
  .prose blockquote {
    border-left: 4px solid #3b82f6;
    background: #f8fafc;
    margin: 2rem 0;
    padding: 1rem 1.5rem;
    border-radius: 0 0.5rem 0.5rem 0;
  }
  
  .prose code {
    background: #f1f5f9;
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.875em;
    color: #1e293b;
    border: 1px solid #e2e8f0;
  }
  
  .prose pre {
    background: #1e293b !important;
    border-radius: 0.5rem;
    padding: 1.5rem;
    overflow-x: auto;
    margin: 2rem 0;
  }
  
  .prose pre code {
    background: transparent;
    border: none;
    padding: 0;
    color: #e2e8f0;
  }
  
  /* Table styling */
  .prose table {
    margin: 2rem 0;
    border-collapse: collapse;
    width: 100%;
  }
  
  .prose th,
  .prose td {
    border: 1px solid #e5e7eb;
    padding: 0.75rem 1rem;
    text-align: left;
  }
  
  .prose th {
    background: #f9fafb;
    font-weight: 600;
  }
  
  /* Image styling */
  .prose img {
    border-radius: 0.5rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    margin: 2rem auto;
  }
  
  /* Link styling */
  .prose a {
    color: #3b82f6;
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }
  
  .prose a:hover {
    border-bottom-color: #3b82f6;
  }
  
  /* List styling */
  .prose ul,
  .prose ol {
    margin: 1.5rem 0;
    padding-left: 1.5rem;
  }
  
  .prose li {
    margin-bottom: 0.5rem;
  }
  
  /* Responsive adjustments */
  @media (max-width: 768px) {
    .prose {
      font-size: 16px;
    }
    
    .prose h1 {
      font-size: 2rem;
    }
    
    .prose h2 {
      font-size: 1.5rem;
    }
    
    .prose pre {
      padding: 1rem;
      margin: 1rem -1rem;
      border-radius: 0;
    }
  }
</style>